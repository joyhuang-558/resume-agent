# OpenRouter API Configuration
# Copy this file to .env and add your actual OpenRouter API key
# Get your key from: https://openrouter.ai/settings/keys

OPENROUTER_API_KEY=your-openrouter-api-key-here

# Knowledge Base Configuration
KNOWLEDGE_TABLE_NAME=knowledge_base
KNOWLEDGE_URI=./knowledge/lancedb

# Embedder Configuration
# Options: "default" (OpenAI), "fastembed" (local), or "openrouter"
EMBEDDER_TYPE=default
FASTEMBED_MODEL=BAAI/bge-small-en-v1.5
OPENROUTER_EMBEDDER_MODEL=openai/text-embedding-3-small

# Chunking Configuration
# Options: "semantic", "document", "fixed_size"
CHUNKING_STRATEGY=semantic
CHUNK_SIZE=5000
CHUNK_OVERLAP=200

# Semantic Chunking Configuration (for resume/PDF files)
# Optimized for resume documents - smaller chunks with semantic boundaries
SEMANTIC_CHUNK_SIZE=500
SIMILARITY_THRESHOLD=0.5

# Dropbox Configuration
DROPBOX_PATH=./dropbox

# LLM Model Configuration (OpenRouter)
# Available models: https://openrouter.ai/models
# Examples:
#   openai/gpt-4o-mini (default, cost-effective)
#   openai/gpt-4o (more capable)
#   anthropic/claude-3.5-sonnet
#   google/gemini-pro-1.5
LLM_MODEL=openai/gpt-4o-mini

# OpenAI API Key (optional, only needed if using default embedder)
# OPENAI_API_KEY=your-openai-api-key-here
